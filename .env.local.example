# Copy this file to .env.local — it is NEVER committed to git.
#
# Because this project uses a Next.js API route as a proxy, the key lives
# only on the server and is NEVER sent to or visible in the browser bundle.
#
# Get your key at: https://console.anthropic.com/

ANTHROPIC_API_KEY=sk-ant-your-key-here
